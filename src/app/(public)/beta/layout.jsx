export const metadata = {
  title: 'Get PXI Beta',
  description:
    'Install PXI on iPhone via Apple TestFlight — two quick steps to join the iOS beta and test new features first.',
  alternates: { canonical: 'https://pxispace.com/beta' },
};

export default function BetaLayout({ children }) {
  return children;
}
