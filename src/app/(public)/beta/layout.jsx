export const metadata = {
  title: 'Get PXI Beta',
  description:
    'Install PXI on iPhone via Apple TestFlight — two quick steps to join the beta.',
};

export default function BetaLayout({ children }) {
  return children;
}
