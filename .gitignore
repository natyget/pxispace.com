# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Production
build/
dist/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env.*
!.env.example

# Vercel
.vercel

# Netlify
.netlify

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Testing
coverage/
.nyc_output/

# Misc
.DS_Store
*.pem
*.p8
.idea/
.vscode/
*.log
# package-lock.json is COMMITTED on purpose. `npm ci` in .github/workflows
# requires it and fails outright without one, and actions/setup-node's
# `cache: npm` has no key to hash. Ignoring it broke every CI run.
.claude/

# Written by `next dev` on every run (see the agentRules option in next.config.js), from the
# docs inside node_modules/next. Generated, not authored: committing them means a diff every
# time Next is upgraded, and they come back on their own for whoever needs them.
AGENTS.md
CLAUDE.md
